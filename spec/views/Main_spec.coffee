describe 'Main View', ->
	it 'should create a main view', ->
	it 'should add a view to main view', ->
	it 'should set an active view', ->
	it 'should return the active view', ->
  it 'should a return a view by name', ->
  it 'should set isActive on a subView', ->
 